<%= form_with(model: technican) do |form| %>
  <% if technican.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(technican.errors.count, "error") %> prohibited this technican from being saved:</h2>

      <ul>
        <% technican.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
